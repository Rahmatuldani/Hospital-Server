import { ValidateMongodbIdPipe } from './validate-mongodb-id.pipe';

describe('ValidateMongodbIdPipe', () => {
  it('should be defined', () => {
    expect(new ValidateMongodbIdPipe()).toBeDefined();
  });
});
